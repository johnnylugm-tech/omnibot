# Omnibot Phase 1~8 Plan Audit (Round 2 — post strip_docstrings review fix)

> **Generated**: 2026-06-26
> **Project**: omnibot
> **Submodule**: harness-methodology @ `0803c12` (was `35d341e`, **6 commits advanced**)
> **Scope**: `.methodology/phase{1..8}_plan.md` (regenerated by `harness_cli.py plan-all --force`)
> **Prior audit**: `omnibot-plan-audit-2026-06-26.md` (F1 + F2 findings)
> **Audit dimensions**: completeness / correctness / consistency

---

## Executive Summary

| 維度 | 結果 | 細節 |
|---|---|---|
| **完整性** | ✅ PASS | 8/8 plans 都有 preamble + 結構化 tags;無遺漏章節 |
| **正確性** | ✅ PASS | **F1 + F2 已修**(P7 +10 行,P8 +16 行,對齊 commit `50a265d`);無新 findings |
| **一致性** | ✅ PASS | 8 plans 全部對齊 6 個新 commits(`0803c12` / `21063df` / `33c320d` / `179c90e` / `3f1fd73` / `50a265d`) |

**Plan regeneration**:8 plans 全部成功重生 (2422 行 total,+26 行 vs round 1),`quality_manifest.json` [PRESERVE] 守衛保護(沒被覆蓋)。
**F1+F2 status**:✅ RESOLVED by `50a265d` fix(plan-generator source fix,not project-side patch)。
**Total findings**:0

---

## 0. Submodule Sync Audit

| 項目 | Round 1 | Round 2 (this) |
|---|---|---|
| Pin | `35d341e` | `0803c12` |
| Commits advanced | 16 | +6 |
| Submodule status | main ✅ | main ✅ |

**新增 commits** (35d341e → 0803c12):
- `0803c12` fix(audit): address 8 review findings on strip_docstrings + get_feature
- `21063df` fix(lint): rename ambiguous 'l' loop var to 'ln' in grep_docstring_aware
- `33c320d` perf(audit): skip AST parsing on clean files + add lru_cache to strip_docstrings
- `179c90e` fix: resolve 5 review findings across 6 files
- `3f1fd73` fix(p8): align CONFIG_RECORDS template vars and fix P8-ARCHIVE cp source
- `50a265d` fix(plan-generator): surface phase8 deterministic baseline in P7/P8 plans

**驗證方法**:`cd harness && git log 35d341e..0803c12 --oneline` → 6 commits confirmed。

---

## 1. Completeness Audit

### 1.1 Plan count ✅ PASS

8/8 phase plans exist after regeneration:

| Plan | Lines (R1) | Lines (R2) | Δ |
|---|---|---|---|
| `phase1_plan.md` | 470 | 470 | 0 |
| `phase2_plan.md` | 520 | 520 | 0 |
| `phase3_plan.md` | 266 | 266 | 0 |
| `phase4_plan.md` | 319 | 319 | 0 |
| `phase5_plan.md` | 178 | 178 | 0 |
| `phase6_plan.md` | 292 | 292 | 0 |
| `phase7_plan.md` | 173 | **183** | **+10** |
| `phase8_plan.md` | 178 | **194** | **+16** |
| **Total** | 2396 | **2422** | **+26** |

**Δ 解讀**:P7 +10、P8 +16 與 commit `50a265d` 的 plan-generator 改動**完美對齊**(commit message 預期 173→183 / 178→194)。

### 1.2 Common preamble ✅ PASS

8/8 plans preamble 一致(都包含):
- `Version: v2.12.0 (project plan)`
- `Project: omnibot`
- `Date: 2026-06-26`
- `Framework: harness-methodology v2.12.0`
- `Phase: N - <phase name>`
- `Status: Full version (including Phase N detailed tasks)`
- `Mode: Dynamic (load-context at execution time)`

Hard rules (HR-04 / HR-05 / HR-16 / HR-17) 在所有 8 plans preamble 一致。

### 1.3 Tag distribution ✅ PASS

P7/P8 的新 tags 已正確生成:

| Plan | Tag 計數 | 驗證重點 |
|---|---|---|
| P7 | 3 hits | `RISK-REGISTER`, `RISK-MITIGATION`, `RISK-STATUS` 對齊 `9958406` |
| P8 | 4 hits | `CONFIG-RECORDS`, `RELEASE-CHECKLIST`, `P8-ARCHIVE`, `P8-HANDOVER-CHECK` 對齊 `4738542` + `3f1fd73` |

### 1.4 FR coverage ✅ PASS(by design)

| Metric | Value |
|---|---|
| Plans reference unique FR-IDs | 3 (`FR-01`, `FR-02`, `FR-99`) — template/sample entries |
| `quality_manifest.json` `fr_ids` | 108 |
| Overlap | 3 (all template) |
| Plans not in manifest | 0 |

**Note**:plans 用 template-style FR references (`FR-01`, `FR-02`, `FR-99` 都是 sample/example);實際 108 個 project-specific FRs 透過 `load-context` 在執行時注入(preamble 明確標示 `Mode: Dynamic`)。Round 1 已驗證此設計,本 round 一致。

---

## 2. Correctness Audit

### F1 (Round 1) — P7 plan 未引用 `phase8_doc_gen.py` 鉤子 ✅ RESOLVED

**Round 1 finding**:P7 plan 描述「advance-phase to P8」時沒提到 P7→P8 自動觸發 `phase8_doc_gen.generate()`。

**Round 2 verification**:
```
.methodology/phase7_plan.md:172:  > **Auto-trigger on P7→P8 advance** (harness commits `4738542` + `51bd4a8`):
.methodology/phase7_plan.md:174:  > `scripts/phase8_doc_gen.py --project .` which deterministically generates
.methodology/phase7_plan.md:175:  > `08-config/CONFIG_RECORDS.md` + `08-config/RELEASE_CHECKLIST.md` from
.methodology/phase7_plan.md:179:  > warning — re-run manually with `python3 harness/scripts/phase8_doc_gen.py --project .`.
```

✅ **RESOLVED** by `50a265d` 的 `_phase_advance_step` 在 P7 plan advance-phase bash block 之後插入 blockquote。

### F2 (Round 1) — P8 plan 未區分 framework-generate vs LLM-generate ✅ RESOLVED

**Round 1 finding**:P8 plan `CONFIG-RECORDS.md` 描述「Generate」,但沒區分 framework 自動產生 vs LLM 補完。

**Round 2 verification**:
```
.methodology/phase8_plan.md:126:> `CONFIG_RECORDS.md` and `RELEASE_CHECKLIST.md` are deterministically generated
.methodology/phase8_plan.md:127:> by `scripts/phase8_doc_gen.py` during the P7→P8 advance-phase hook (see
.methodology/phase8_plan.md:128:> `phase7_plan.md` §Auto-trigger on P7→P8 advance). P8 phase work is therefore
.methodology/phase8_plan.md:138:- **[CONFIG-RECORDS]** Review + append `08-config/CONFIG_RECORDS.md`:
.methodology/phase8_plan.md:144:- **[RELEASE-CHECKLIST]** Review + append `08-config/RELEASE_CHECKLIST.md`:
```

✅ **RESOLVED** — P8 plan 現在明確標示 deliverables 為 "Review + append" 而非 "Generate",並 cross-reference P7 plan §Auto-trigger。

### New findings (R2):0

對 6 個新 commits 逐一檢查對 plans 的影響:

| Commit | 影響 plans? | 驗證 |
|---|---|---|
| `0803c12` strip_docstrings AST refactor + 8 review fixes | ❌ Framework 內部改動 (audit helper + harness_config docstring) | 計畫不引用內部 helpers,合預期 |
| `21063df` rename 'l' → 'ln' | ❌ Lint rename | 計畫無涉及 |
| `33c320d` perf(audit) lru_cache + skip AST | ❌ Performance 改動 | 計畫無涉及 |
| `179c90e` 5 review fixes across 6 files | ❌ 跨檔 fixes (audit/config/bridge/tool_runners/drift_detector),都是 framework 內部 | 計畫不涉及 |
| `3f1fd73` CONFIG_RECORDS template vars + P8-ARCHIVE cp source | ✅ P8 plan | `[P8-ARCHIVE]` tag 在 phase8_plan.md:152 驗證;template vars 由 generator 自動處理 |
| `50a265d` plan-generator surface phase8 baseline | ✅ P7 + P8 plan | F1 + F2 修好驗證 (見上) |

### Command reference audit ✅ PASS

Harness CLI 命令 references 統計(全 8 plans 累計):

| Command | References | 評估 |
|---|---|---|
| `run-phase` | 多 phase | ✅ Core workflow |
| `advance-phase` | 多 phase | ✅ Core workflow |
| `load-context` | 多 phase | ✅ Dynamic mode |
| `run-gate` / `finalize-gate` | 多 phase | ✅ Gate workflow |
| `run-env-check` / `finalize-env-check` | 5 + 5 | ✅ Environment verification |
| `spec-coverage-check` | 11 | ✅ D4 spec-coverage |
| `check-test-mirrors-spec` | 1 | ✅ P3 gate |
| `check-test-spec-consistency` | 1 | ✅ P2 gate |
| `build-trace-attestation` | 7 | ✅ Trace 4a |
| `validate-handoff` | 7 | ✅ Phase handoff |
| `resume-fr-phase` | 5 | ✅ Crash recovery |
| `bug-hunt-targets` | 1 | ✅ P4 bug hunt |
| `mutation-test-score` | 多 phase | ✅ Mutation testing |
| `push-milestone` | 多 phase | ✅ Git push |
| `push-checkpoint` | 多 phase | ✅ Git push |
| `check-constitution` | 多 phase | ✅ Constitution check |
| `init-project` | 多 phase | ✅ Init |
| `dispatch` | 1 | ✅ Agent B dispatch |
| `run-tool` | 多 phase | ✅ Tool runners |

所有命令在 submodule 的 `harness/harness_cli.py` 都已實作 — 無 dangling references。

---

## 3. Consistency Audit

### 3.1 Cross-plan artifact references ✅ PASS

P(N) → P(N-1) deliverables 引用一致:

| 從 | 到 | Deliverable |
|---|---|---|
| P2 | P1 | SRS.md / SPEC_TRACKING.md / TRACEABILITY_MATRIX.md / TEST_INVENTORY.yaml |
| P3 | P2 | TEST_SPEC.md |
| P4 | P3 | per-FR Gate 1 sentinels |
| P5 | P4 | TEST_RESULTS.md |
| P6 | P5 | VERIFICATION_REPORT.md |
| P7 | P6 | QUALITY_REPORT.md + RELEASE_NOTES.md + FINAL_SIGN_OFF.md + quality_manifest.json |
| P8 | P7 | RISK_REGISTER.md + RISK_MITIGATION_PLANS.md + RISK_STATUS_REPORT.md |

### 3.2 P7 → P8 transition consistency ✅ PASS(R1 was ⚠️, now fixed)

| Plan 對象 | 一致性 (R1) | 一致性 (R2) |
|---|---|---|
| P7 advance-phase | ⚠️ 未提到 P7→P8 自動觸發 | ✅ 明確提到 phase8_doc_gen |
| P8 entry | ✅ 提到 CONFIG_RECORDS.md + RELEASE_CHECKLIST.md | ✅ 提到 deliverables 已就緒(framework-generated baseline) |

**Net improvement**:P7↔P8 transition 在 plan 文件層面 fully consistent,執行者不再需要反向 source dive 才能理解 framework auto-behavior。

### 3.3 Manifest structure alignment ✅ PASS

`quality_manifest.json` 結構對齊 `d95fff6`:
- `fr_module_traceability`: 108 entries
- `fr_ids`: 108 entries
- `nfr_dimension_mapping` / `nfr_fr_mapping` / `nfr_traceability`: NFR 全套對齊
- `gate_results.gate1`: 115 entries (108 real + 7 sample)

[PRESERVE] 守衛保護(對齊 `8445e91`)— 沒被覆蓋。

### 3.4 Plan vs framework version ✅ PASS

| Plan 標頭 | Framework commit 對應 |
|---|---|
| `v2.12.0 (project plan)` | harness-methodology main @ `0803c12` |
| 6 commits advanced (vs R1) | `35d341e` → `0803c12` |
| Date 2026-06-26 | 對齊 submodule 更新時間 |

### 3.5 Hard rules consistency ✅ PASS

HR-04 / HR-05 / HR-16 / HR-17 在所有 8 個 plans preamble 一致。

---

## 4. Diff Summary: Round 1 → Round 2

| Item | Round 1 (`35d341e`) | Round 2 (`0803c12`) |
|---|---|---|
| Total plan lines | 2396 | 2422 (+26) |
| F1 status | ❌ unresolved | ✅ resolved |
| F2 status | ❌ unresolved | ✅ resolved |
| New findings | 0 | 0 |
| Plan regeneration mechanism | manual review + project patch | generator-level fix (`50a265d`) |
| Quality manifest [PRESERVE] | ✅ | ✅ |

**關鍵觀察**:F1 + F2 在 round 1 是 project-side patches(round 1 自己寫進 `.methodology/`),但 `50a265d` 把它移到 generator source(`scripts/generate_full_plan.py`)。現在 generator 是 single source of truth,project 不再需要手動 patch。

---

## 5. Recommendations

### 5.1 立即可做 — 0 件

無未解決 findings。F1 + F2 已由 `50a265d` 從 source 修好,plans 自動反映。

### 5.2 後續可考慮 — 1 件

| # | 動作 | 優先級 |
|---|---|---|
| 1 | 在 omnibot CI 加 pre-commit hook 驗證 plan regeneration 後 line counts 與 manifest [PRESERVE] 守衛 — 防止 generator 未來 regression | low (defensive) |

### 5.3 不建議

| 動作 | 為什麼不做 |
|---|---|
| 把 `0803c12` / `179c90e` 等內部 fixes 寫進 plan | Plans 應關注 workflow + deliverables,不引用 framework 內部 helper 改動;round 1 已驗證此原則 |
| 修 generator source 把 6 個新 commits 的細節都 surface 進 plan | 過度耦合 — plans 應保持 template 性質,project-specific 改動由 dynamic load 處理 |

---

## 6. Self-Review

### 可能錯誤之處
1. **假設 FR coverage 是 by-design**:可能漏掉某個 plan 應該 reference 特定 FR-IDs(例如 Phase 3 應該 ref FR-01..FR-05 的測試 mirrors)。但 round 1 已驗證 dynamic load 是設計意圖,且 manifest `fr_ids=108` 反映 project 實際需求。
2. **命令 reference 統計可能漏算**:grep 是 `harness_cli.py <cmd>`,可能漏掉 inline 寫法(如 `python3 harness_cli.py`)。但所有命令都有 prefix 一致性,風險低。
3. **「F1 + F2 已 RESOLVED」可能過度樂觀**:確認 `phase7_plan.md:172-179` 內容與 `50a265d` diff 一致,但未驗證執行時 advance-phase 是否真的呼叫 `phase8_doc_gen` 成功(屬於 framework 行為驗證,非 plan 文檔驗證)。

### 未驗證假設

| 假設 | 狀態 |
|---|---|
| 108 FRs 是完整集合 | Partial verified (與 round 1 一致) |
| Plan regeneration 不會破壞 in-progress task tracking | Unverified (`[x]` progress marks 在 plan-all --force 後可能重置) |
| 6 個新 commits 都已 merged 到 origin/main | Verified (`git log 35d341e..0803c12`) |

### 信心等級

| 維度 | 等級 | 依據 |
|---|---|---|
| 完整性 | **High** | 8 plans 結構、tag 分布、preamble 已逐項驗證 |
| 正確性 F1+F2 RESOLVED | **High** | 直接 grep 驗證 + commit diff 對齊 |
| 一致性 | **High** | cross-plan references + manifest 對齊全部已驗證 |
| FR coverage (dynamic load) | **Medium** | 設計意圖已驗證,實際 load 行為未驗證 |

### 如果結論錯了,最可能錯在哪?

- **若 F1/F2 仍存在**:可能 grep 看的 line numbers 偏移(plan regeneration 後行號會變) — 已對齊內容,風險低
- **若 plan 行數不符**:可能 generator 在 round 1 之後又改 — 已對齊 commit `50a265d` 預期 +10/+16,風險低
- **若 6 commits 沒全部 merge**:可能 submodule 有未推送 commit — `git fetch` 已執行,當前 submodule HEAD == origin/main HEAD,風險低

---

## Appendix A: Round 1 → Round 2 Diff Highlights

### P7 plan diff (173 → 183)

新增 lines 172-181 (10 lines):
```markdown
> **Auto-trigger on P7→P8 advance** (harness commits `4738542` + `51bd4a8`):
> When `advance-phase --completed 7` runs, the framework automatically invokes
> `scripts/phase8_doc_gen.py --project .` which deterministically generates
> `08-config/CONFIG_RECORDS.md` + `08-config/RELEASE_CHECKLIST.md` from
> the `quality_manifest.json` + `tests/gate_results.jsonl` artifacts.
> The generator is idempotent (byte-equal on re-run with unchanged inputs).
> If it fails, advance-phase still SUCCESS with a non-blocking
> warning — re-run manually with `python3 harness/scripts/phase8_doc_gen.py --project .`.
```

### P8 plan diff (178 → 194)

新增 lines 125-130 (6 lines, baseline source blockquote):
```markdown
> `CONFIG_RECORDS.md` and `RELEASE_CHECKLIST.md` are deterministically generated
> by `scripts/phase8_doc_gen.py` during the P7→P8 advance-phase hook (see
> `phase7_plan.md` §Auto-trigger on P7→P8 advance). P8 phase work is therefore
> **review and append**, not generation: verify the baseline captures every FR
> configuration, then append any human-only context the generator cannot
> derive (escalation notes, manual sign-offs, release-specific overrides).
```

Reframed `[CONFIG-RECORDS]` / `[RELEASE-CHECKLIST]` tags (lines 138, 144) 從「Generate」改為「Review + append」(其他 8 lines)。

### Other 6 plans: 無改動 (對齊 commit message「Verified by regenerating omnibot's 8 phase plans」)

---

## Appendix B: Commit Cross-Reference (R2)

| Finding | Commit | Title |
|---|---|---|
| F1 RESOLVED | `50a265d` | fix(plan-generator): surface phase8 deterministic baseline in P7/P8 plans |
| F2 RESOLVED | `50a265d` | (同上) |
| P8 plan template var alignment | `3f1fd73` | fix(p8): align CONFIG_RECORDS template vars and fix P8-ARCHIVE cp source |
| (no impact on plans) | `0803c12` | fix(audit): address 8 review findings on strip_docstrings + get_feature |
| (no impact on plans) | `21063df` | fix(lint): rename ambiguous 'l' loop var to 'ln' in grep_docstring_aware |
| (no impact on plans) | `33c320d` | perf(audit): skip AST parsing on clean files + add lru_cache to strip_docstrings |
| (no impact on plans) | `179c90e` | fix: resolve 5 review findings across 6 files |

---

## Appendix C: Self-Audit Methodology

1. **完整性**:`wc -l` 對 8 plans;grep preamble + hard rules;tag 分布計數
2. **正確性**:
   - F1/F2 grep 驗證
   - 6 commits 對 plans 逐一 trace 影響
   - Harness CLI 命令 reference 統計(對 submodule 已實作命令)
3. **一致性**:
   - Cross-plan artifact references
   - P7↔P8 transition
   - Manifest 結構
   - Plan vs framework version
   - Hard rules

**Cross-references with round 1**:0 new findings vs 2 resolved — net improvement。