# CONFIG_RECORDS.md - omnibot

> Phase 8 Configuration Management deliverable.
> Baseline auto-generated by `harness/scripts/phase8_doc_gen.py` (commit 4738542 + 51bd4a8).
> Framework baseline was incomplete (P8-BUG-01: template field-name mismatch — `{hash}`/`{date}` vs context `git_hash`/`release_date`).
> Values below were filled manually per plan §CONFIG-RECORDS "Flag any missing sections the generator could not derive".
> Human-only sections appended at the bottom.

## 1. Version Information
- Version: v0.1.0 (pyproject.toml)
- Git Tag: harness-v4-20260625-score100
- Git Commit: 015c1ef (handover: advance to Phase 8)
- Release Date: 2026-06-26

## 2. Runtime Configuration
| Environment | Config |
|-------------|--------|
| Development | `.env` (local); `.env.example` (template); pyproject.toml [project] |
| Production | CI/CD injected secrets; deployment manifest (deployment/k8s/) |

## 3. Dependency List
- Python 3.11 (.venv)
- See `pyproject.toml` [project.dependencies] for runtime deps
- See `pyproject.toml` [project.optional-dependencies.dev] for dev tools (ruff, mypy, pyright, pytest, bandit, mutmut, gitleaks via brew)
- See `uv.lock` for pinned versions

## 4. Environment Variables
| Variable | Type | Description |
|----------|------|-------------|
| OPENAI_API_KEY | secret | OpenAI / OpenAI-compatible API key |
| OPENAI_BASE_URL | url | Override the default OpenAI endpoint |
| MINIMAX_API_KEY | secret | MiniMax embedding native endpoint |
| GEMINI_API_KEY | secret | Gemini fallback LLM |
| FALLBACK_LLM_MODEL | string | LLM fallback model (default: gemini-1.5-flash) |
| DATABASE_URL | connection string | postgresql+asyncpg://user:pass@host:port/db |
| REDIS_URL | connection string | rediss:// for TLS, redis:// for plain |
| CLAMAV_HOST | hostname | ClamAV daemon host |
| CLAMAV_PORT | port | ClamAV daemon port (default 3310) |
| JWT_SECRET_KEY | secret | 32+ char HMAC secret |
| M2M_SECRET_KEY | secret | 32+ char M2M token signing key |
| IP_WHITELIST_CIDRS | cidrs | CIDR list, comma separated |
| MESSENGER_VERIFY_TOKEN | secret | Facebook Messenger challenge verifier |
| WHATSAPP_VERIFY_TOKEN | secret | WhatsApp Cloud API challenge verifier |
| A2A_JWKS_URL | url | A2A JSON Web Key Set endpoint |
| A2A_AUDIENCE | string | A2A token `aud` claim |
| OTEL_EXPORTER_OTLP_ENDPOINT | url | OpenTelemetry collector (default http://127.0.0.1:4318) |
| PROMETHEUS_PORT | port | Prometheus metrics port (default 8001) |
| APP_ENV | enum | development / staging / production |
| LOG_LEVEL | enum | DEBUG / INFO / WARNING / ERROR |
| TESTING | flag | "true" when running tests |

## 5. Deployment Log
| Date | Version | Method | Executor |
|------|---------|--------|----------|
| 2026-06-25 | v0.1.0 | handover commit `015c1ef` (Phase 7→8 advance) | @johnnylugm-tech |
| 2026-06-26 | v0.1.0 | (pending) Phase 8 PUSH ⑩ | @johnnylugm-tech |

## 6. Configuration Change Log
| Phase | Change | Rationale |
|-------|--------|-----------|
| Phase 8 | pip-licenses shim re-located (P7 deferred → P8) | Per RISK-006; shim still lives in venv due to non-PyPI binary issue |
| Phase 7 | mutmut mutation testing excluded | Per RISK-002; user instruction; feature flag disabled in harness_config.py |
| Phase 6 | finalize-gate quality_complete/passed/verdict post-patch | Per P6-BUG-13 (framework); workaround in gate4_result.json post-finalize |
| Phase 6 | readability_v2 metric (LLOC-weighted CC) | Per RISK-001; replaces Halstead MI (submodule 9aa6df7) |

## 7. Rollback SOP
**Trigger Condition**: Gate 4 composite < 85 OR any critical/high issue surfaces post-PUSH ⑩.

**Commands**:
```bash
# Roll back to last known-good tag
git tag -l 'harness-v4-*'                    # list known-good tags
git checkout harness-v4-20260625-score100    # last P6 Gate 4 PASS
# Rebuild environment
.venv/bin/python -m pip install -e ".[dev]"
# Restart services
kubectl rollout undo deployment/omnibot      # or platform-specific
```

**Owner**: @johnnylugm-tech (project lead)

## 8. Configuration Compliance
- [x] Phase 7 risk mitigations implemented (RISK-001 CLOSED, RISK-002 OPEN/P8 watchlist)
- [x] Monitoring thresholds configured (NFR-11 ≥99.95% early warning; NFR-12 latency alert >800ms; NFR-13 error_rate >0.5% alert)
- [x] Circuit breaker enabled (`app.infra.circuit_breaker`, NFR-10 ≥99.9% uptime)

---

## 9. Human-Only Context (Append per plan §CONFIG-RECORDS)

### 9.1 Ownership per Config Item

| Component | Primary Owner | Backup |
|-----------|---------------|--------|
| `app.core.paladin` (FR-10..17) | @johnnylugm-tech | (TBD) |
| `app.core.knowledge` (FR-26..33) | @johnnylugm-tech | (TBD) |
| `app.core.dst` (FR-34..38) | @johnnylugm-tech | (TBD) |
| `app.infra.circuit_breaker` (FR-99) | @johnnylugm-tech | (TBD) |
| `app.infra.redis_streams` (FR-80,81,90) | @johnnylugm-tech | (TBD) |
| `app.infra.rate_limit` (FR-21..25) | @johnnylugm-tech | (TBD) |
| `app.infra.jobs` (FR-75..79) | @johnnylugm-tech | (TBD) |
| `app.services.aee` (FR-39..43,45) | @johnnylugm-tech | (TBD) |
| `app.services.llm_judge` (FR-65..69) | @johnnylugm-tech | (TBD) |
| `app.services.media` (FR-100) | @johnnylugm-tech | (TBD) |
| Secret rotation cadence | @johnnylugm-tech (M2M 90-day expiry per NFR-36) | — |
| Access audit log reference | `.methodology/sessions_spawn.log` + `.methodology/decision_logs/` | — |

### 9.2 Secret Rotation Cadence

| Secret | Rotation | Overlap |
|--------|----------|---------|
| M2M_SECRET_KEY | 90 days (NFR-36) | 24hr overlap on rotate |
| JWT_SECRET_KEY | 90 days | 24hr overlap on rotate |
| OPENAI_API_KEY | 180 days | Immediate swap (no overlap window) |
| GEMINI_API_KEY | 180 days | Immediate swap |
| MINIMAX_API_KEY | 180 days | Immediate swap |
| WEBHOOK_VERIFY_TOKENS | On provider rotation event | N/A (provider-managed) |

### 9.3 Access Audit Log Reference

- `.methodology/sessions_spawn.log` — AgentSpawner auto-populated (non-blocking debug trail)
- `.methodology/decision_logs/YYYY-MM-DD/GATE_N_NNN.yaml` — Phase 6+ gate decisions
- `.methodology/fr_progress.json` — per-FR Gate 1 status with timestamps
- `.methodology/gate_timestamps.jsonl` — gate completion timestamps

### 9.4 Known Limitations (from RISK_REGISTER)

- **RISK-002 OPEN**: Mutation testing feature flag disabled; no automated mutation score. Interim: 100% line coverage + A3 reviews.
- **RISK-006 MITIGATED**: pip-licenses shim lives in venv (P6-BUG-10); recreate steps in `.audit/harness-bugs-phase6.md`. Migrate to PyPI when upstream fixes module entry-point.
- **RISK-003 MITIGATED**: `gate4_result.json` dimensions vs composite score reconciled by framework DA waivers + NFR overrides (audit trail in `.methodology/audit_gaps_6.md`).

### 9.5 Framework Bugs Documented (Phase 4-8)

| Phase | Bugs | Status |
|-------|-----:|--------|
| P4 | 6 (H1-H6) | All hotfixed |
| P5 | 5 (P5-BUG-01..05) | All hotfixed (7660996) |
| P6 | 17 (P6-BUG-01..17) | 4 upstream PRs landed, 1 pending (P6-BUG-13) |
| P7 | 0 | First zero-framework-bug phase |
| **P8** | **1 (P8-BUG-01, in this phase)** | Documented; workaround applied; upstream fix pending |

P8-BUG-01: `phase8_doc_gen.py` template field-name mismatch. Safe-substitute leaves literals. Workaround: manual fill in CONFIG_RECORDS.md.

---

**Status**: Configuration Management Phase 8 deliverable complete.
**Approver**: @johnnylugm-tech (project lead)
**Last update**: 2026-06-26