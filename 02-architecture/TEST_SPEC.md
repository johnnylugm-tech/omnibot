# TEST_SPEC.md — Named Test-Case Catalog (Single Source of Truth)

<!-- harness:template-stub -->
<!-- Remove the sentinel line above once you start filling this TEST_SPEC.
     While present, harness load-context emits a stub warning. -->

> **Phase 2 deliverable.** Generated by Agent A (ARCHITECT) following the
> `derive_test_cases.md` skill protocol. Test names from P1 TEST_INVENTORY.yaml
> are preserved as-is where specified. This is the SINGLE SOURCE OF TRUTH for
> all test traceability checks at Gates 1-4 (v2.6 unified D4). Validated by
> Agent B (TECH_LEAD) before P2 exit. P3 Agent A implements tests FROM this
> catalog — not ad-hoc.

**Project**: <!-- project name -->  
**SRS version**: <!-- git SHA or date of SRS.md used -->  
**SAD version**: <!-- git SHA or date of SAD.md used -->  
**TEST_INVENTORY.yaml version**: <!-- git SHA or date of TEST_INVENTORY.yaml used for naming -->
**Generated**: <!-- YYYY-MM-DD -->  
**Active NFR Patterns**: <!-- NP-01, NP-02, ... filled by Step 1 of skill -->

---

## NFR Pattern Activation (Step 1 + Step 1b Output)

<!-- The table MUST enumerate ALL 15 patterns (NP-01…NP-15) from
     derive_test_cases.md Step 1 — a shortened table caused real pattern
     drift (a project shipped concurrency bugs because NP-13 was absent from
     its copy of this table). The Trigger column records the source:
     "SRS: <keyword>" (Step 1) or "SAD: <module>" (Step 1b architecture-risk —
     fires REGARDLESS of SRS keywords; its cases are mandatory and need
     integration variants under tests/integration/).

| Pattern | Trigger (SRS keyword / SAD module) | Activated |
|---------|-------------------------------------|-----------|
| NP-01 (auth 401)          | not found                  | ❌ |
| NP-02 (authz 403)         | not found                  | ❌ |
| NP-03 (rate limit 429)    | not found                  | ❌ |
| NP-04 (validation 422)    | SRS: "input validation"    | ✅ |
| NP-05 (idempotency)       | not found                  | ❌ |
| NP-06 (latency SLA)       | SRS: "p95"                 | ✅ |
| NP-07 (dependency fault)  | SAD: redis_cache (cache)   | ✅ |
| NP-08 (security attack)   | not found                  | ❌ |
| NP-09 (audit log)         | not found                  | ❌ |
| NP-10 (data round-trip)   | not found                  | ❌ |
| NP-11 (backward compat)   | not found                  | ❌ |
| NP-12 (pagination)        | not found                  | ❌ |
| NP-13 (concurrency)       | SAD: circuit_breaker (shared state + async) | ✅ |
| NP-14 (encryption)        | not found                  | ❌ |
| NP-15 (timeout)           | SAD: audio_converter (external process)    | ✅ |
-->

---

## Functional Requirement Test Cases

<!-- One section per FR from SRS §2. Generate using derive_test_cases.md 7-Q protocol. -->

### FR-01: <!-- one-line description from SRS §2 -->

**Classification**: <!-- API_ENDPOINT | DATA_ENTITY | ALGORITHM | STATE_MACHINE | INTEGRATION | SECURITY_CONTROL | INFRASTRUCTURE -->  
**Active Patterns**: <!-- NP-01, NP-04 | none -->

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr01_` | source="視頻"; expected="影片" | happy_path | Q1 |
| 2 | `test_fr01_` | source="和"; expected="ㄏㄢˋ" | validation | Q2 |

<!-- Inputs column: concrete declared values as key="value", semicolon-separated.
     MUST be the TRUE value (e.g. expected="ㄌㄜˋ ㄙㄜˋ" with a real space), NOT the
     pytest-id form (ㄌㄜˋ_ㄙㄜˋ). A case with only a descriptive id and no concrete
     Inputs is REJECTED — it lets P3 invent values and mis-count (the FR-03
     all-boundary bug). P3 implements these verbatim.

     Test Function naming is LANGUAGE-INDEPENDENT (D4 spec-coverage matches the
     name set verbatim):
       python — function name:   def test_fr01_happy_path():
       js/ts  — test TITLE:      it('test_fr01_happy_path', () => { ... })
                                 test("test_fr01_happy_path", ...) also accepted.
     The names declared here are the single source — implementations must use
     them verbatim in either form. -->

**Sub-assertions** — predicate over a case's declared Inputs / the production
`result`. **Predicates are ALWAYS Python-expression syntax** (e.g.
`len(result) == 4`), regardless of the implementation language — they are
spec-level declarations evaluated by the P2 self-consistency engine, not test
code. For js/ts projects the P3 mirror gate checks structure only and flags
each predicate as needs_review for the human reviewer. The P2 self-consistency gate
(`harness_cli.py check-test-spec-consistency`) proves every predicate holds for
every case in `applies_to`; an unsatisfiable group (e.g. `" " in "ㄏㄢˋ"` False,
or 4 one-char chunks for a 5-char input) FAILS P2 — correctness is locked HERE,
before P3. List a case in `applies_to` only if the predicate truly holds for its
Inputs.

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC5-bopomofo-space | `" " in expected` | 3 |

<!--
Repeat pattern for FR-02, FR-03, ... FR-NN.
Each FR needs at minimum:
  - 1 happy_path (Q1, mandatory)
  - 1 validation or failure (Q2, mandatory)
  - boundary (Q3) if numeric limits exist
  - state_transition (Q4) if STATE_MACHINE
  - fault_injection (Q5) if INTEGRATION
  - nfr_pattern (Q6) for each active NP-XX relevant to this FR
  - integration (Q7) if FR output feeds another FR
Each FR also needs:
  - an Inputs value for every case (concrete, true-form, not the id)
  - a Sub-assertions table whose predicates are self-consistent for the Inputs
-->

---

## Cross-Cutting Test Cases

### NFR Integration

<!-- System-wide NFR tests not tied to a single FR -->

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 1 | `test_app_starts_and_health_endpoint_returns_200` | smoke | deployment |

### Backward Compatibility

<!-- Present only for multi-phase projects. Delete section if not applicable. -->

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 1 | `test_phase1_contract_satisfied_in_phase2` | backward_compat | NP-11 |

### Deployment Smoke

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 1 | `test_app_starts_and_health_endpoint_returns_200` | smoke | deployment |

---

## Summary

| Metric | Count |
|---|---|
| FRs covered | 0 |
| Total test cases | 0 |
| By type: happy_path | 0 |
| By type: validation/failure | 0 |
| By type: boundary | 0 |
| By type: state_transition | 0 |
| By type: fault_injection | 0 |
| By type: nfr_pattern | 0 |
| By type: integration | 0 |
| By type: smoke | 0 |
| Active NFR patterns applied | — |

---

*Generated by: derive_test_cases.md v1.1 | harness-methodology v2.7.0*
